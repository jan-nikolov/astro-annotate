---
import { ClientRouter } from 'astro:transitions';
const title = 'About — astro-annotate Playground';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <ClientRouter />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { font-family: system-ui, sans-serif; color: #1a1a2e; }
      nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 16px 20px;
        background: white;
        border-bottom: 1px solid #eee;
      }
      nav a { color: #1a1a2e; text-decoration: none; margin-left: 24px; }
      main {
        max-width: 700px;
        margin: 0 auto;
        padding: 60px 20px;
      }
      h1 { font-size: 2.5rem; margin-bottom: 24px; }
      p { line-height: 1.8; margin-bottom: 16px; color: #444; }
      ul { margin: 16px 0 16px 24px; }
      li { line-height: 1.8; color: #444; margin-bottom: 8px; }
      .highlight {
        background: #fff3cd;
        padding: 16px 20px;
        border-radius: 8px;
        border-left: 4px solid #ffc107;
        margin: 24px 0;
      }
      footer {
        text-align: center;
        padding: 24px;
        color: #888;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <nav>
      <strong>Playground</strong>
      <div>
        <a href="/">Home</a>
        <a href="/about">About</a>
      </div>
    </nav>

    <main>
      <h1>About This Playground</h1>
      <p>
        This is a test page for the astro-annotate integration. It exists to verify
        that annotations work correctly across different page layouts and elements.
      </p>

      <div class="highlight">
        <strong>Try annotating this box!</strong> Click the Annotate button in the
        bottom-right corner, then click on this highlighted section.
      </div>

      <h2>What to test</h2>
      <ul>
        <li>Hover highlighting shows the correct element</li>
        <li>CSS selectors are generated correctly</li>
        <li>Annotations persist after page reload</li>
        <li>Pin markers appear on annotated elements</li>
        <li>Status changes (resolve, reopen) work</li>
      </ul>

      <p id="unique-paragraph">
        This paragraph has a unique ID, so the selector generator should use
        <code>#unique-paragraph</code> as the selector.
      </p>

      <p>
        This paragraph has no ID, so the selector generator will use a
        tag + class or nth-child path instead.
      </p>
    </main>

    <footer>
      <p>astro-annotate playground — for testing purposes only</p>
    </footer>
  </body>
</html>
